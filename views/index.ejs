<% include partials/header %>
        <div class="container">
            <div class="row">
                <div class="col">
                    <% include partials/nav %>
                </div>
            </div>
            <div class="row">
                <div class="col">
                <% if(users) { %>
                    <h3>Existing users</h3>
                    <ul>
                    <% for(user of users) { %>
                        <li> User name: <a href='/user/<%= user._id %>'><%= user.name %></a> Age: <%= user.age %> | <a href="/user/delete/<%= user._id %>">Delete user</a></li>
                    <% } %>
                    </ul>
                <% } %>
                <a href="/user/new" title=''>Add New User</a>
                </div>
                <!-- <div class="col-6">
                    <form action="/user" class="user-form" method="post">
                        <div class="form-group">
                            <label for="name">Your name: </label>
                            <input class='form-control' type="text" name="name" placeholder="">
                        </div>
                        <div class="form-group">
                            <label for="age">Your age: </label>
                            <input class='form-control' type="text" name="age" placeholder="">
                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary">Submit</button>
                        </div>
                    </form>
                </div> -->
            </div>
        </div>
<% include partials/footer %>